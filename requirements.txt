# Orchestrator + Portfolio-Analyst-Agent (compute_portfolio, fetch_prices)
pandas>=1.5.0
numpy>=1.22
yfinance>=0.2.0

# FastAPI + Swagger/OpenAPI
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Scheduling (interval or cron)
apscheduler>=3.10.0

# Agents are imported from sibling dirs (Market-Regime-Agent, Capital-Allocation-Agent, Portfolio-Analyst-Agent).
