# Orchestrator + Portfolio-Analyst-Agent (compute_portfolio, fetch_prices)
pandas>=1.5.0
numpy>=1.22
yfinance>=0.2.0

# FastAPI + Swagger/OpenAPI (python-multipart required for form/file uploads)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Scheduling (interval or cron)
apscheduler>=3.10.0

# Backtest and data service client
requests>=2.28.0
httpx>=0.27.0

# Database (Supabase / pgvector when needed)
psycopg2-binary>=2.9.0
pgvector>=0.2.0
python-dotenv>=1.0.0

# Skill traversal embeddings
openai>=1.0.0

# Agents are imported from sibling dirs (Market-Regime-Agent, Capital-Allocation-Agent, Portfolio-Analyst-Agent).
# Backtest requires qtos-core as sibling (see POST /backtest).
